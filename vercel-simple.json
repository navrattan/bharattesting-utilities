{
  "functions": {
    "app/**/*.dart": {
      "runtime": "@vercel/static-build@1.3.39"
    }
  },
  "buildCommand": "curl -L https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.29.0-stable.tar.xz | tar -xJ -C /tmp && export PATH=/tmp/flutter/bin:$PATH && cd app && flutter config --enable-web --no-analytics && flutter pub get && flutter build web --release",
  "outputDirectory": "app/build/web",
  "installCommand": "echo 'Skipping npm install'",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}