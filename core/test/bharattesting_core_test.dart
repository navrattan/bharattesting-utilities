import 'package:test/test.dart';
import 'package:bharattesting_core/bharattesting_core.dart';

void main() {
  group('BharatTesting Core', () {
    test('library imports work', () {
      // Basic smoke test to ensure the library can be imported
      expect(1 + 1, equals(2));
    });
  });
}